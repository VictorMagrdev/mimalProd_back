spring:
  datasource:
    url: minimalProd:postgresql://localhost:5432/minimalProd
    username: minimal_prod_user
    password: minimal_prod_pass
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true

jwt:
  secret: my-very-secret-key-change-me
  expiration-ms: 3600000

flyway:
  enabled: true
  locations: classpath:db/migration
